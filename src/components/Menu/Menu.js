import React from 'react';
import PropTypes from 'prop-types';
import './Menu.css';

const Menu = () => (
  <div className="Menu">
    Menu Component
  </div>
);

Menu.propTypes = {};

Menu.defaultProps = {};

export default Menu;
